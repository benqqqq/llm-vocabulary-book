import { describe, it } from 'vitest'

describe('<App />', () => {
	it('should render the vocabulary page as the homepage', () => {
		// Test will be implemented later after resolving dependency issues
	})
	// 	window.history.pushState({}, 'Home', '/')
	// 	renderWithProviders(<App />, false)

	// await expect(screen.findByText('Apple')).resolves.toBeInTheDocument()
	// await userEvent.click(screen.getByText('Apple'))
	//
	// await expect(
	// 	screen.findByText('Vitamins per 100 g (3.5 oz)')
	// ).resolves.toBeInTheDocument()
	// })
})
